General Notes and Troubleshooting

Problem: apt-get update and install was failing (it was hanging at 0%)
Solution: restart docker machine VM

Use `apt-cache policy <packageName>` to detect which available packages there are

Possibly worth looking at (extension of docker): https://github.com/grammarly/rocker

Problem: How to source a python virtual environment (or source anything?) in the context of a bash script
Answer: ????
!! This is important. It was also preventing me from using nvm
